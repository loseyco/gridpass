Reviewing your component implementation:

1. **Hover State**: Instead of using `hover:bg-amber-400`, I'd recommend using accessibility classes like `hover:opacity-90` or `hover:text-black/75` for better contrast while maintaining readability.

2. **Whitespace and Spacing**: The button has minimal padding which could be improved with a proper padding class (e.g., `px-3 py-1.5`). This will also help with the text alignment.

3. **Icon Size**: The Zap icon is quite small at its current size (`w-4 h-4`). I'd suggest increasing it to `w-6 h-6` for better visibility.

Here's how you could update your component:


<button className="bg-amber-500 hover:bg-amber-500/90 transition-all text-black font-bold px-3 py-1.5 rounded">
  <Zap className="w-6 h-6" />
  Action
</button>


Key changes:
- Removed the color change on hover in favor of opacity change for better accessibility
- Added proper padding to improve spacing and readability
- Increased icon size slightly
- Kept the same transition effect but with a more accessible hover state